agot_spawn_historical_dragon_skulls_effect = {
	# Some foreword:
	# There were 19 dragon skulls in King's Landing so we can't add all of them
	# Following were chosen to be omitted:

	# MODCANON DRAGONS
	# Volon, Vaedar, Telarion, Nessarion, Nightfyre, Anogaros

	# SEMICANON DRAGONS
	# Valryon, Ghiscar, Essovius, Archonei
	# I know they are explicitly mentioned by Viserys as the skulls in the Throne Room but there's simply no room for them
	# I would have to expel full-canon dragons out of the list to make space and i want to avoid that

	# CANON DRAGONS
	# Vermax, Stormcloud, Sheepstealer, Silverwing, Quicksilver

	$OWNER$ ?= {
		if = { # Essovius
			limit = {
				exists = character:dragon_vermithrax
				character:dragon_vermithrax ?= { is_alive = no }
			}
			agot_create_artifact_dragon_skull_effect = {
				OWNER = this
				DRAGON = character:dragon_vermithrax
				KILLER = character:dragon_vermithrax
			}
		}
		if = { # Balerion the Black Dread
			limit = {
				exists = character:dragon_balerion
				character:dragon_balerion ?= { is_alive = no }
			}
			agot_create_artifact_dragon_skull_effect = {
				OWNER = this
				DRAGON = character:dragon_balerion
				KILLER = character:dragon_balerion
			}
		}
		if = { # Meraxes
			limit = {
				exists = character:dragon_meraxes
				character:dragon_meraxes ?= { is_alive = no }
			}
			if = {
				limit = { game_start_date > 8013.6.1 }
				agot_create_artifact_dragon_skull_effect = {
					OWNER = this
					DRAGON = character:dragon_meraxes
					KILLER = character:Uller_19 # Uthor Uller
				}
			}
			else = {
				title:d_brimstone.holder ?= {
					agot_create_artifact_dragon_skull_effect = {
						OWNER = this
						DRAGON = character:dragon_meraxes
						KILLER = character:Uller_19 # Uthor Uller
					}
				}
			}
		}
		if = { # Vhagar
			limit = {
				exists = character:dragon_vhagar
				character:dragon_vhagar ?= { is_alive = no }
			}
			agot_create_artifact_dragon_skull_effect = {
				OWNER = this
				DRAGON = character:dragon_vhagar
				KILLER = character:dragon_caraxes
			}
		}
		if = { # Dreamfyre
			limit = {
				exists = character:dragon_dreamfyre
				character:dragon_dreamfyre ?= { is_alive = no }
			}
			agot_create_artifact_dragon_skull_effect = {
				OWNER = this
				DRAGON = character:dragon_dreamfyre
				KILLER = character:dragon_dreamfyre
			}
		}
		if = { # Vermithor the Bronze Fury
			limit = {
				exists = character:dragon_vermithor
				character:dragon_vermithor ?= { is_alive = no }
			}
			agot_create_artifact_dragon_skull_effect = {
				OWNER = this
				DRAGON = character:dragon_vermithor
				KILLER = character:dragon_seasmoke # Seasmoke
			}
		}
		if = { # Meleys the Red Queen
			limit = {
				exists = character:dragon_meleys
				character:dragon_meleys ?= { is_alive = no }
			}
			agot_create_artifact_dragon_skull_effect = {
				OWNER = this
				DRAGON = character:dragon_meleys
				KILLER = character:dragon_vhagar # Vhagar
			}
		}
		if = { # Caraxes the Blood Wyrm
			limit = {
				exists = character:dragon_caraxes
				character:dragon_caraxes ?= { is_alive = no }
			}
			agot_create_artifact_dragon_skull_effect = {
				OWNER = this
				DRAGON = character:dragon_caraxes
				KILLER = character:dragon_vhagar # Vhagar
			}
		}
		if = { # Sunfyre the Golden
			limit = {
				exists = character:dragon_sunfyre
				character:dragon_sunfyre ?= { is_alive = no }
			}
			agot_create_artifact_dragon_skull_effect = {
				OWNER = this
				DRAGON = character:dragon_sunfyre
				KILLER = character:dragon_sunfyre
			}
		}
		if = { # Seasmoke
			limit = {
				exists = character:dragon_seasmoke
				character:dragon_seasmoke ?= { is_alive = no }
			}
			agot_create_artifact_dragon_skull_effect = {
				OWNER = this
				DRAGON = character:dragon_seasmoke
				KILLER = character:dragon_vermithor # Vermithor
			}
		}
		if = { # Syrax
			limit = {
				exists = character:dragon_syrax
				character:dragon_syrax ?= { is_alive = no }
			}
			agot_create_artifact_dragon_skull_effect = {
				OWNER = this
				DRAGON = character:dragon_syrax
				KILLER = character:dragon_syrax
			}
		}
		if = { # Moondancer
			limit = {
				exists = character:dragon_moondancer
				character:dragon_moondancer ?= { is_alive = no }
			}
			agot_create_artifact_dragon_skull_effect = {
				OWNER = this
				DRAGON = character:dragon_moondancer
				KILLER = character:dragon_sunfyre # Sunfyre the Golden
			}
		}
		if = { # Tessarion the Blue Queen
			limit = {
				exists = character:dragon_tessarion
				character:dragon_tessarion ?= { is_alive = no }
			}
			agot_create_artifact_dragon_skull_effect = {
				OWNER = this
				DRAGON = character:dragon_tessarion
				KILLER = character:Burley_16 # Billy Burley
			}
		}
		if = { # Shrykos
			limit = {
				exists = character:dragon_shrykos
				character:dragon_shrykos ?= { is_alive = no }
			}
			agot_create_artifact_dragon_skull_effect = {
				OWNER = this
				DRAGON = character:dragon_shrykos
				KILLER = character:Lowborn_Kings_Landing_2 # Hobb the Hewer
			}
		}
		if = { # Morghul
			limit = {
				exists = character:dragon_morghul
				character:dragon_morghul ?= { is_alive = no }
			}
			agot_create_artifact_dragon_skull_effect = {
				OWNER = this
				DRAGON = character:dragon_morghul
				KILLER = character:dragon_morghul
			}
		}
		if = { # Arrax
			limit = {
				exists = character:dragon_arrax
				character:dragon_arrax ?= { is_alive = no }
			}
			agot_create_artifact_dragon_skull_effect = {
				OWNER = this
				DRAGON = character:dragon_arrax
				KILLER = character:dragon_vhagar # Vhagar
			}
		}
		if = { # Tyraxes
			limit = {
				exists = character:dragon_tyraxes
				character:dragon_tyraxes ?= { is_alive = no }
			}
			agot_create_artifact_dragon_skull_effect = {
				OWNER = this
				DRAGON = character:dragon_tyraxes
				KILLER = character:dragon_tyraxes
			}
		}
		if = { # Morning
			limit = {
				exists = character:dragon_morning
				character:dragon_morning ?= { is_alive = no }
			}
			agot_create_artifact_dragon_skull_effect = {
				OWNER = this
				DRAGON = character:dragon_morning
				KILLER = character:dragon_morning
			}
		}
		if = { # The Last Dragon
			limit = {
				exists = character:dragon_the_last
				character:dragon_the_last ?= { is_alive = no }
			}
			agot_create_artifact_dragon_skull_effect = {
				OWNER = this
				DRAGON = character:dragon_the_last
				KILLER = character:dragon_the_last
			}
		}
	}
}